{
  "name": "confeitaria-mika",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de gestão de pedidos e tarefas para confeitaria",
  "scripts": {
    "setup": "npm install --prefix backend && npm install --prefix frontend",
    "docker:up": "docker compose --env-file .env.local up -d",
    "docker:down": "docker compose --env-file .env.local down",
    "db:migrate": "npm run prisma:migrate:dev --prefix backend",
    "db:seed": "npm run --prefix backend prisma:seed",
    "dev:backend": "npm run start:dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
